import {TrainingInstance} from "../../training/training-instance";

export class TrainingInstancePagination {

  constructor(trainingInstance: TrainingInstance, page: number, size: number, sortBy: string) {
  }
}
