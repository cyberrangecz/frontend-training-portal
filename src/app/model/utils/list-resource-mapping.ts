export interface ListResourceMapping {
  /**
   * Name of the attribute of mapped resource to be used as id
   */
  id: string;

  /**
   * Name of the attribute of mapped resource to be used as title
   */
  title: string;
}
