export class AgendaMenuItem {
  icon: string;
  label: string;
  path: string;

  constructor(icon: string, label: string, path: string) {
    this.icon = icon;
    this.label = label;
    this.path = path;
  }
}
